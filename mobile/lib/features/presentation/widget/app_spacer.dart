import 'package:flutter/material.dart';

class AppPaddingSpacer extends StatelessWidget {
  const AppPaddingSpacer({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 12,
      height: 12,
    );
  }
}
